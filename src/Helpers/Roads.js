import {Vector2D} from "@inwebo/vector";

export default class Roads {
    /**
     * @param {Vector2D} chunkDimension
     */
    constructor(chunkDimension) {
        this._chunkDimension = chunkDimension;
    }

    build() {

    }
}